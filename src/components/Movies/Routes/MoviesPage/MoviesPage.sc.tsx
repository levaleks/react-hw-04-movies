import styled from 'styled-components';

export const SMoviesPage = styled.div`
    form {
        display: flex;

        input {
            flex-grow: 1;
            flex-shrink: 0;
            margin-right: 15px;
        }
    }
`;
